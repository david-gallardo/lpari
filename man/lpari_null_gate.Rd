% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null-gate.R
\name{lpari_null_gate}
\alias{lpari_null_gate}
\title{Evaluate the conservative null gate before LPA-RI interpretation}
\usage{
lpari_null_gate(
  x,
  criteria = c("BIC", "CAIC"),
  min_delta = 0,
  require_all = TRUE,
  min_class_size = NULL,
  min_class_prop = NULL
)
}
\arguments{
\item{x}{A candidate table or an \code{lpari_result} object.}

\item{criteria}{Character vector of information criteria used for the gate.
Defaults to \code{c("BIC", "CAIC")}.}

\item{min_delta}{Minimum improvement required for a multiclass model over
the one-profile model on each criterion. For example, \code{10} requires the
best multiclass criterion value to be at least 10 points lower than K = 1.
The default \code{0} asks only whether the criterion selects K > 1.}

\item{require_all}{Logical; if \code{TRUE}, all criteria must pass. If \code{FALSE},
at least one criterion must pass.}

\item{min_class_size}{Optional minimum class size for the best multiclass
model under each criterion.}

\item{min_class_prop}{Optional minimum class proportion for the best
multiclass model under each criterion.}
}
\value{
A data frame with one row per task and a \code{gate_pass} decision.
}
\description{
LPA-RI is calibrated for difficult multiclass enumeration and should not be
used as a stand-alone test that latent profiles exist. This helper implements
a conservative gate: retain the one-profile solution unless conventional
information criteria provide enough evidence that a multiclass solution
improves over K = 1.
}
